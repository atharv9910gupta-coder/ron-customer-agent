RON — Premium Customer Support (frontend)

Quick steps:
1. Create repo and copy frontend/ files.
2. Add `GROQ_API_KEY` to Streamlit Secrets (or env) for AI replies.
3. Optionally add Supabase credentials (SUPABASE_URL, SUPABASE_KEY) to use cloud DB.
4. Upload `assets/logo.png` to repo if you want a logo.
5. Deploy frontend with Streamlit Cloud or run locally:
   pip install -r requirements.txt
   streamlit run frontend/app.py
